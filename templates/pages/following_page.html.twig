{% extends "base.html.twig" %}

{% block following %}
<div class="page--following">
    <div class="section__list">
        <div class="container__navigation">
            <a href="/profile">
                <img src="{{ asset('/images/user_icon.png') }}"   alt="">
            </a>
            <a href="/feed">
                <img src="{{ asset('/images/feed_icon.png') }}"  alt="">
            </a>
            <a href="/people">
                <img src="{{ asset('/images/team_icon.png') }}"  alt="">
            </a>
            <a href="/messages">
                <img src="{{ asset('/images/comment_icon.png') }}"  alt="">
            </a>
        </div>
        <h1>Following</h1>
        {% if following|length > 0 %}
            {% for follow in following %}
                {% include 'elements/profile_tab_follow.html.twig' with {'username': follow.username, 'lastName' : follow.lastName, 'id' : follow.id, 'firstName' : follow.firstName} %}
            {% endfor %}
        {% else %}
            <h3>@{{user.username}} doesn't follow anyone</h3>
        {% endif %}
    </div>
</div>
{% endblock %}


